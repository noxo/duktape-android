ext {
  compileSdkVersion = 26
  buildToolsVersion = '26.0.2'
  buildGradleVersion = '8.5.1'
  ndkAbiFilters = ['x86', 'x86_64', 'armeabi-v7a', 'arm64-v8a']
}

allprojects {
  buildscript {
    repositories {
      mavenCentral()
      google()
    }
    dependencies {
      classpath "com.android.tools.build:gradle:$buildGradleVersion"
    }
  }
  repositories {
    mavenCentral()
    google()
  }
}
